
   // import {createApp} from 'vue';
   //
   // import App from './App.vue';
   //
   // createApp(App).mount('#root')

   import {createApp} from 'vue';

   import App from './App.vue';

   // 引入插件
   // import res from 'vue-resource';
   
   const app = createApp(App)
   app.mount('#root')
   // app.use(res)


   // createApp(App).use(vueResource).mount('#root')

